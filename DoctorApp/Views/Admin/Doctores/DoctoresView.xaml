<Window x:Class="DoctorApp.Views.Admin.Doctores.DoctoresView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DoctorApp.Views.Admin.Doctores"
        xmlns:vm ="clr-namespace:DoctorApp.ViewModels"
        mc:Ignorable="d"
        Title="DoctoresView" Height="450" Width="800">
    <Window.DataContext>
        <vm:DoctoresViewModel></vm:DoctoresViewModel>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="799*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#FFEEEEEE" Grid.ColumnSpan="2">
            <Image Source="/Assets/Logo.jpg" Height="50" VerticalAlignment="Center" Margin="10"/>
            <TextBlock Text="Hospital General de la Región Carbonífera" FontSize="24" VerticalAlignment="Center" Margin="10,0,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Background="#FFDDDDDD" Grid.ColumnSpan="2" Margin="0,0,0,344" Grid.RowSpan="2">
            <Button Content="Usuarios" Command="{Binding VerUsuariosCommand}" Margin="0,0,10,0"/>
            <Button Content="Salas" Command="{Binding VerSalasCommand}" Margin="0,0,10,0"/>
        </StackPanel>

        <Grid Grid.Row="2" Margin="10,10,9,10">
            <Button Content="Agregar" Command="{Binding VerAgregarCommand}" Width="50" Height="50" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,10,10,0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Image Source="/Assets/Vistasadmin/images/Agregar.png" />
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <ListView Margin="0,60,0,0" Name="DoctorList" ItemsSource="{Binding Usuarios}" SelectedItem="{Binding UsuarioSeleccionado}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Doctores" Width="260" DisplayMemberBinding="{Binding Nombre}" />
                        <GridViewColumn Header="Pacientes atendidos" Width="200">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Path=DataContext.PacientesAtendidos[Id], RelativeSource={RelativeSource AncestorType={x:Type ListView}}}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Tiempo Promedio de espera del Paciente" Width="300" DisplayMemberBinding="{Binding PacientesAtendidos}" />
                        <GridViewColumn Header="Acciones" Width="300" 
                                        DisplayMemberBinding="{Binding PacientesAtendidos}">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Button Content="Editar" Command="{Binding DataContext.VerEditarCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}"/>
                                        <Button Grid.Column="1" Content="Eliminar" Command="{Binding DataContext.VerEliminarCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}"/>
                                    </Grid>
                                 </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </Grid>
</Window>
